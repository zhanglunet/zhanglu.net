---
name: "query-business-ontology"
description: |
  Read and analyze the current Excel-derived evidence and business ontology without reopening Excel. Use first for requests such as “分析Excel、做一下体检” when the workspace already contains committed evidence, and for object discovery, definitions, values, trust, lineage, reconciliation, attribution, ad hoc read-only exploration across current case data, What-if history, handover packages, and RDF/OWL export. Do not use to build ontology definitions, change standard-data availability, approve semantics, or execute real business Actions.
source: local
featured: false
handwritten: false
synced_at: "2026-08-16"
---

本 skill 来源于本机 `~/.claude/skills/query-business-ontology/SKILL.md`，由 `pnpm run sync:skills` 自动同步。

## 描述

Read and analyze the current Excel-derived evidence and business ontology without reopening Excel. Use first for requests such as “分析Excel、做一下体检” when the workspace already contains committed evidence, and for object discovery, definitions, values, trust, lineage, reconciliation, attribution, ad hoc read-only exploration across current case data, What-if history, handover packages, and RDF/OWL export. Do not use to build ontology definitions, change standard-data availability, approve semantics, or execute real business Actions.
