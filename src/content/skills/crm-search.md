---
name: "aic-crm-search"
description: |
  crm-search 服务 CLI skill：该服务为CRM系统提供统一的搜索能力，支持按钮触发的分类检索（SCA、SAF、PRF等业务模块）以及跨模块的全局搜索功能。 当用户要操作该服务时使用。
source: local
featured: false
handwritten: false
synced_at: "2026-07-27"
---

本 skill 来源于本机 `~/.claude/skills/crm-search/SKILL.md`，由 `pnpm run sync:skills` 自动同步。

## 描述

crm-search 服务 CLI skill：该服务为CRM系统提供统一的搜索能力，支持按钮触发的分类检索（SCA、SAF、PRF等业务模块）以及跨模块的全局搜索功能。 当用户要操作该服务时使用。
