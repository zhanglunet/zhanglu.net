# 2026-08-06 · 全站审查 + 下一阶段 PRD 与开发计划

## 目标

对仓库代码、文档、线上站点做一次全量审查，产出下一阶段的需求文档（PRD）与开发计划。
这是一次「只审不改」的盘点：本次交付物是文档，审查发现的问题留给 PRD 里的批次去修。

## 方法

远程 agent 会话内用多 agent workflow 执行（11 个子 agent）：

1. **6 路并行审计**：内容集合（zh/en 逐 slug 对齐、frontmatter 质量、§11 快照）/ 页面组件（镜像
   完整性、ui.ts key 对齐、硬编码）/ agent 机读层（端点-builder-manifest-llms.txt-CLI 五方对齐）/
   脚本基建（§5.4.2 五坑防护逐行核实、CI 缺口、发版积压）/ 文档层（22 条 dev-log 通读挖未竟事项）/
   线上状态（全程 `curl --noproxy '*'` 直连，§9.13 铁律）。
2. **3 个视角提案**：产品读者 / 工程维护 / agent 接口，各出候选需求，共 25 项。
3. **对抗校验**：逐项回仓库核实证据、查重、对照 §10 / §14.5 / §16.3 红线。
   结果 confirmed 20 / adjust 5 / refuted 0；两对跨视角重复提案（冒烟脚本、CLI 0.2.1）合并。

## 改动（文件清单）

- `docs/prd-2026-08.md` —— 新增。PRD 正文：现状评估、23 项需求（R1-R23）、5 个批次的开发计划、
  用户侧清单、不做清单。
- `docs/dev-log/2026-08-06-site-review-prd.md` —— 本文。

不改任何 `src/` / `public/` / `scripts/` / `cli/` 内容。

## 验证

- `pnpm build` 通过（163 页，zh 81 + en 81 + 404），确认纯文档改动未误伤 `src/`。
- 审查本身的核验：线上 counts 与仓库逐项对账一致、robots.txt 无 Cloudflare 注入、
  npm `zhanglu-net@0.2.0` dist-tag 核实、`generated_at` 在 HEAD 后 48 秒（线上=最新 main）。

## 踩坑 / 值得记的发现

- **审计仅有的两个 high 都在 `auto-sync-skills.sh` 的 git 逻辑里**（skillsEn 删除不进 commit、
  untracked 新增被 `git diff --quiet` 判无变化），此前 §5.4.2 五坑没覆盖到——修复进 PRD R1，
  落地时应把这两坑补进 AGENTS §9 或 §5.4.2。
- 「无实质变化不改写」逻辑失效的根因不是逻辑缺失，是 gray-matter 解析 `description: |`
  块标量带尾随 `\n` 导致比较恒不等——对抗校验纠正了提案的初诊，修复时别重写整段。
- 容器里跑 `sync:check` 比对的是**容器自己的** `~/.claude/skills`，对判断用户 Mac 的漂移无意义
  （§9.12 的推论，提案初版想让容器出漂移清单，被校验员否掉）。
- 对抗校验环节实际拦下/修正了 5 项提案的事实错误，这套「提案不直接进 PRD、先回仓库对证」的
  流程值得沿用。

## 结论与交付物

- 交付：`docs/prd-2026-08.md`（23 项需求、5 批次计划、用户侧清单）。
- 总体判断：站点底子健康（双语 1:1、机读层零漂移、线上=main、五坑防护属实），
  问题集中在 auto-sync git 缺陷（高危）、零 CI、发版/文档积压、OG 覆盖、posts 无机读出口。
- 下一步：按 PRD 批次 1（修错止血，R1-R4/R14）开工。
