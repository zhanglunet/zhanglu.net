# 2026-07-18 · 发版 v0.2.0 + 发版流程文档化 + README/截图刷新

> 回溯补记（提交 `9aa5b00`、`6e232fe`、`f978626`、`a1236ad`、`fb73c6a`）。

## 目标

发一个 v0.2.0 里程碑，并把 GitHub README 与预览截图刷新。

## 改动

- `package.json` — version `0.1.0` → `0.2.0`（`9aa5b00`）。
- `CHANGELOG.md` — 新增，v0.2.0 发布说明（`6e232fe`）。
- `AGENTS.md` §8.1 — 补发版流程，并加固 gh 路径（`f978626`、`a1236ad`）。
- `README.md` + `docs/screenshots/{home,projects,about,skills}.png` — 刷新预览截图 + 补 loc 字段说明（`fb73c6a`）。

## 验证

- `pnpm build` 过。
- 截图为构建后各页实拍，README 内联引用确认可显示。

## 踩坑（重要，已固化进 §8.1）

- **本会话类型（Claude Code on the web / 远程 agent）建不了 GitHub Release、也推不了 tag**：Release API 返回 `not permitted for this session type`，git 代理对 tag ref push 返回 403。**这是会话类型的分类限制，放开仓库权限也没用，别空转重试。**
- 处理：agent 做完 ①bump ②写 CHANGELOG ③提交上 main；第 ④ 步（tag + Release）留给人在 GitHub UI（`/releases/new?tag=vX.Y.Z&target=main`）或本地 gh 完成。
- gh 发布只抽本版段落做 notes（`--notes-file CHANGELOG.md` 会把整个 changelog 塞进去）；本地要先 `git merge --ff-only origin/main` 才有新加的 CHANGELOG。

## 结论与交付物

版本升到 0.2.0，CHANGELOG + 发版 SOP 入库；tag/Release 待人工在 GitHub 完成。
